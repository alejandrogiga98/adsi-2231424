<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider within a group which
| contains the "web" middleware group. Now create something great!
|
*/

Route::get('/', function () {
    return view('welcome');
});

Route::get('hello', function () {
    return "<h1> Welcome ADSI learner</h1>";
});

Route::get('example/users', function () {
    $users = App\Models\User::all();
    dd($users); //dumb and die
});
Route::get('example/challenge', function () {
    $users = App\Models\User::all();
    return view('challenge')->with('users', $users);
});

Route::get('ageTest', function () {
    $user = App\Models\User::find(2);
    $actual = now();
    echo $actual;
    echo "<br>";
    echo $user->birthdate;
    echo "<br>";
    //echo $actual->diffInYears($user->birthdate);
    echo now()->diffInYears($user->birthdate);

    //return $user;
    //return "<h1> Welcome ADSI learner</h1>";
});
Route::get('createdTest', function () {
    $user = App\Models\User::find(3);
    $actual = now();
    echo "actual: ",$actual;
    echo "<br>";
    echo "creacion: ",$user->created_at;
    echo "<br>";
    echo now()->diffInWeeks($user->created_at);
    //echo $dif->toDateString;
    //echo $actual->diffInYears($user->birthdate);
    //echo now()->diffInYears($user->birthdate);

    //return $user;
    //return "<h1> Welcome ADSI learner</h1>";
});
